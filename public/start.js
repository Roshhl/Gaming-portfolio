document.addEventListener('keydown', function (e) {
  if (e.key.toLowerCase() === 'a') {
    window.location.href = './loading.html';
  }
  else if (e.key.toLowerCase() === 'tap') {
    window.location.href = './loading.html';
  }
});
